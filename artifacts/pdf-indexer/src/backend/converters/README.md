# Converter Backend Hook

Use this folder for server-side DOCX, XLSX, and PPTX renderers when deploying a
full backend. Recommended Python libraries include `python-docx`, `openpyxl`,
`python-pptx`, PyMuPDF, pdfplumber, pdfminer.six, OpenCV, and layoutparser.
